from selenium import webdriver
import time
from selenium.webdriver.common.by import By
import pandas as pd
import numpy as np
driver = webdriver.Chrome()
try:
    driver.get(url='https://bingx.com/en/spot/BTCUSDT/')
    time.sleep(3)
    price = driver.find_element(By.CSS_SELECTOR, '#__layout > div > div.spot-container > div.content-container > div > div > div.vue-grid-item.vue-resizable.symbol-header > div > div > div > span:nth-child(2) > span > span').text
    print(price)
    time.sleep(5)
except Exception as ex:
    print(ex.__class__.__name__)
finally:
    driver.close()
    driver.quit(